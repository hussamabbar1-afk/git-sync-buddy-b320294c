// Acquisition is live, while billing remains independently fail-closed in the
// Stripe Edge Functions until the Gewerbe and test credentials are available.
export const acquisitionCampaignLive = true;
